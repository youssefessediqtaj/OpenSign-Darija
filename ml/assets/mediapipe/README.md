# MediaPipe Assets

This directory is the native deterministic location for local MediaPipe task assets used by OpenSigne Darija ML preprocessing.

`holistic_landmarker.task` is intentionally ignored by Git. Generate or refresh it with:

```bash
make ml-download-mediapipe
```
