# MediaPipe Assets

This directory is the native deterministic location for local MediaPipe task assets used by OpenSigne Darija ML preprocessing.

`holistic_landmarker.task` is intentionally ignored by Git because it is a local binary
asset. Place the reviewed task file here before running preprocessing or Docker
validation; the current local-only workflow does not provide a downloader.
