# Word Dataset Processing

Mendeley MoSL v1 is processed as `WORD_ISOLATED`.

The pipeline discovers MP4 videos, preserves original labels, normalizes Arabic Unicode, records provenance, and refuses signer-independent claims unless signer IDs are actually recoverable per file.
