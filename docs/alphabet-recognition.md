# Alphabet Recognition

Alphabet recognition is a separate `ALPHABET_STATIC` task. It must not be trained together with isolated word videos.

The first baseline uses hand landmarks, stabilization, explicit confirmation, and cooldown before adding letters to a word.
